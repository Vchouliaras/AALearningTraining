import React, {Component} from 'react';
import {Platform, StyleSheet, Text, View} from 'react-native';

class Styling extends Component {
  render() {
    return (
      <View>
        <Text>Welcome to React Native styling</Text>
      </View>
    )
  }
}

export default Styling